# Real-Time Object Detection using YOLOv8

This project implements a real-time object detection system using the YOLOv8 Nano model. It captures live video input from a webcam and performs fast, accurate object detection with bounding boxes and class labels.

## Installation
```bash
git clone https://github.com/Simrannn-26/new-detection.git
cd new-detection
pip install -r requirements.txt

pip install -r requirements.txt


#usage
python realtime_detector.py
